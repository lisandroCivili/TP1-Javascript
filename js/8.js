const number = parseInt(prompt("Comprueba si un numero es divisible por 2:"));

if (number%2 === 0) {
    document.write(number + " si es divisible por 2.")
} else {
    document.write(number + " no es divisible por 2.")
}